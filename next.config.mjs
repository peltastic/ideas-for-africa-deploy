/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "ideas-for-africa-backend.onrender.com",
            "res.cloudinary.com"
        ]
    }
};

export default nextConfig;
